spring:
  datasource:
    url:
      jdbc:mysql://db4free.net:3306/testbooks
    username:
      thaissantos
    password:
      thaissantos
  jpa:
    hibernate:
      ddl-auto:
        create

server:
  error:
    include-message:
      always
logging:
  level:
    org:
      hibernate:
        SQL:
          DEBUG
        type:
          descriptor:
            sql:
              BasicBinder:
                TRACE